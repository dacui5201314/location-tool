current_phase: "Launch Stabilization 2026-05-28"
phase_status: "main_analyze_chain_working_ui_payment_admin_remaining"

repo:
  project: "C:\\Users\\admin\\location-tool"
  committed_head_before_handoff_slimming: "082c43d"
  latest_code_commit: "6a55fce"
  latest_backend_fix_commit: "668ccf7"
  launch_target: "uniapp WeChat Mini Program"
  frontend_policy: "frontend/ is product master/reference; do not modify unless explicitly approved"

verified_working:
  - "Native switchTab white flash fixed by single-page shell/custom bottom nav"
  - "Map no longer overlays other tabs or disappears after fast tab switching"
  - "Favorites -> Analyze -> Report chain works with favorite_id/address/lat/lng/write-back"
  - "User manually verified favorite id=6 can read/open report_uuid 6413ae9793384520ae0fb90dbe24930f"
  - "/api/analyze succeeded end-to-end with P0/P2/P3 hard guard preserved"
  - "Report detail opens after successful analyze"
  - "Report-detail mini-program display UI polished without changing report data/scoring/POI/prompt/fact guard/accuracy logic"
  - "Mini-program global UI first polish pass completed: clearer favorite button, traditional bottom tabbar, profile/cards/button states, records/favorites/profile visual polish"
  - "Home compactAddress is display-only for top store-location label; full address remains in current-location area and original addressText/analyze payload must remain unchanged"
  - "share_token report sharing exists"

successful_report:
  report_uuid: "6413ae9793384520ae0fb90dbe24930f"
  analysis_record_id: 96
  user_id: 46
  favorite_id: 6
  favorite_latest_report_uuid_written: true
  location: "Shaanxi Baoji"
  industry: "xiaochi kuaican"
  brand_name: "shan er ya gan mian pi"
  store_size: 50
  member_tier: "yearly"
  billing: "member analyze did not deduct points; no refund created for successful run"

open_issues:
  p0: []
  p1:
    - "WeChat Pay production credential setup and direct purchase flow"
    - "Analyze 5-run same-input stability acceptance"
  p2:
    - "Final mini-program visual QA: bottom tabbar safe area, long text, empty/loading states, compact address display, and report-detail overlap checks"
    - "Admin UI/IA cleanup and industry prompt participation audit"
    - "P0-NAME artifact maintenance for future narrow false-positive filters"
    - "HarmonyOS compatibility QA"
    - "Draggable map pointer request, limited by WeChat map capabilities"

hard_boundaries:
  - "Do not push unless explicitly asked"
  - "Do not leak AppID/AppSecret/tokens/API keys/AMap keys/security secrets/merchant keys/certs/PEM/private keys"
  - "Do not process tmp_*"
  - "Do not add /unlock-pdf or /download"
  - "Do not implement PDF now"
  - "Do not weaken P0/P2/P3 fact guard"
  - "Do not turn real P0 into warning-save"
  - "Do not modify scoring, POI classification, prompt semantics, report accuracy logic, or report_fact_guard unless explicitly authorized"
  - "Do not modify frontend/ unless explicitly approved"

next_priority:
  - "Inspect git status first"
  - "Final QA mini-program UI polish already done in uniapp/: report-detail, home, favorites, records, profile, traditional bottom tabbar"
  - "Verify bottom tabbar safe-area spacing, long text, empty/loading states, and home compactAddress display on real device/DevTools"
  - "Keep compactAddress display-only; do not change addressText/addressKeyword/analyze payload/favorites/map/regeocode"
  - "Payment direct purchase after credentials are provided"
  - "Admin IA/UI after explicit frontend approval"

validation:
  backend_changed: "uv run python -m compileall backend"
  uniapp_changed: "cd uniapp && npm run build:mp-weixin"
  frontend_changed_with_permission: "cd frontend && npm run build"
  required_scans:
    - "JSON parse package/manifest/pages/dist app.json/project.config.json"
    - "rg \"unlock-pdf|/download\" uniapp/src == 0"
    - "rg \"moveToLocation|moveToMapLocation\" uniapp/src uniapp/dist/build/mp-weixin == 0"
    - "rg \"switchTab\" uniapp/src uniapp/dist/build/mp-weixin == 0"
    - "payment chain remains requestPayment/createPrepay/queryOrder"
    - "share chain keeps share_token/onShareAppMessage"
    - "developer/debug copy scan"
    - "secret scan"
    - "git status"
