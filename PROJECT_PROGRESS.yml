current_phase: Phase 13
phase_status: ready_for_billing_and_save_chain_hardening
latest_code_commit: 52cc749
latest_code_commit_message: "feat: complete pre-launch accuracy hardening"
latest_docs_commit: 53813cd
unpushed: true

validation:
  compileall: PASS
  industry: "2168 PASS, 0 FAIL"
  fact_guard: "101 PASS, 0 FAIL"
  known_rule_gaps: none

phase12_changes:
  - "火锅_烧烤 sub_first fixed: removed 大排档/排档 from substitute, added 酒楼"
  - "AB invariant: all masters/subtypes with amap_codes must explicitly req_kw=True"
  - "categories_excluded added to 5 masters (烘焙/民宿/低频零售/专业生活/社区基础); 中餐 intentionally N/A"
  - "_check_sentence tolerance narrowed: 3x -> max(e+3, e*2)"
  - "Prohibited decision language detection added (7 patterns -> fact_errors)"
  - "Single-point financial number detection added (-> fact_errors)"
  - "Display chain verified: DB/HTML/frontend/API proxy consistent"

intentionally_partial:
  - "10 sample bank groups lack sufficient substitute samples (stable national definitions unavailable)"
  - "radius fallback (1000m default) noted as future optimization"
  - "Few masters still lack master-level sub_first (non-blocking)"

real_report_validation:
  db_analysis_records_total: 74
  phase9_10_saved_reports: 37
  direct_smoke_verification: 3
  invalid_empty_poi_samples: 3
  fact_errors_triggered: 12
  retry_salvaged: 9
  retry_failed: 1
  actual_refunds: 2

sample_bank:
  complete_candidate: 12
  partial: 10
  latest_industry_test_total: "2168 PASS, 0 FAIL"

phase13_focus:
  - "Report accuracy mainline is complete; do not reopen POI/rules unless a concrete regression is found."
  - "Fix blocking billing/save-chain risks before launch: analysis DB save failure must not return success; PDF unlock concurrent double-charge must refund or avoid charging."
  - "Worktree may contain uncommitted partial backend/main.py, backend/routers/records.py, and backend/tests/check_report_fact_guard.py Phase 13 attempts; verify/refund-wire them before committing code."
  - "Add minimal tests for the billing/refund/save failure paths if feasible without broad refactor."
  - "Dead-code cleanup is optional; do not touch config.py PROVIDER_CONFIG unless legacy ai_providers imports are migrated."

next_step: "Phase 13: harden billing refund and report save failure paths, then proceed to frontend product acceptance / small traffic."

boundaries:
  no_push: true
  no_frontend_ui_pdf: true
  no_database_schema_changes: true
  do_not_process_tmp_artifacts: true
  no_more_forced_sample_completion: true
