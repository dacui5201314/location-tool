// 正式小程序后端域名：上传审核前确认微信 request 合法域名已配置同源 HTTPS。
// 注意：不使用 import.meta.env，其 Vite 编译产物在微信小程序中会生成 require("url") 导致所有页面空白

const API_BASE_URL = 'https://www.oliver188.top'

export default { API_BASE_URL }
